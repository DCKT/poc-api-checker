{
  "name": "bs-api-checker",
  "version": "0.1.0",
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w",
    "clean": "bsb -clean-world",
    "api": "json-server --watch api.json",
    "test": "npm run build && node examples/axios_test.bs.js"
  },
  "keywords": ["BuckleScript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "bs-axios": "0.0.36",
    "bs-fetch": "^0.3.0",
    "bs-platform": "^2.2.3",
    "json-server": "^0.13.0"
  },
  "dependencies": {
    "bs-json": "^1.0.1"
  }
}
